[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
	# deploy_dir = deploy
	# revert_dir = revert
	# verify_dir = verify
	# extension = sql
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "joskuijpers"]
	uri = db:pg:joskuijpers
[core "pg"]
	target = joskuijpers
[deploy]
	verify = true
[rebase]
	verify = true
